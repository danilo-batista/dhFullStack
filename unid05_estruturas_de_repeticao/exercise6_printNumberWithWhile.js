"use strict";

/**
 * Crie um laço de repetição While que irá iterar enquanto o valor de i for menor ou
igual a 15 e mostre no console o valor de cada iteração.
 */
let counter = 1;

while (counter <= 15) {
    console.log(counter);
    counter++;
}