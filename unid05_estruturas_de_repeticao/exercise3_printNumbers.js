"use strict";

/**
 * Crie um loop que execute 15 repetições e cada ciclo desse laço deve imprimir o
valor da variável contadora, que nesse caso irá começar no 1.
Por exemplo, o resultado no terminal precisa ser esse:
1
2
3
4… e assim por diante.
 */
for (counter = 1; counter <= 15; counter++) {
    console.log(counter);
}